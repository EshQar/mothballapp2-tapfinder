[package]
name = "brute_forcer"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "brute_forcer"
crate-type = ["cdylib"]

[dependencies]
itertools = "0.15.0"
pyo3 = "0.29.0"

[profile.profiling]
inherits = "release"
debug = true